[project]
name = "bot"
version = "0.1.0"
description = "LMS Telegram Bot"
dependencies = [
    "python-dotenv",
    "httpx",
    "aiogram",
    "python-telegram-bot>=22.7",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
